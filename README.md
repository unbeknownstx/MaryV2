MaryV2 13.2 Agency → Autonomy proposal bridge

Copy these two files over the prior convergence-patched tree while preserving paths:
- mary/runtime/application.py
- tests/runtime/test_mary_application.py

No /data files are included or changed.
