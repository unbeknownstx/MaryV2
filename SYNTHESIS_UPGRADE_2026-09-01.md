# MaryV2 Character Runtime Synthesis Upgrade — 2026-09-01

This source snapshot is a non-destructive evolution of the previous green Character Runtime upgrade. It mines public AI-character/VTuber/NPC projects for useful mechanisms while preserving one canonical Mary Core and existing ownership boundaries.

## Deterministic checkpoint

- Full pytest: **1602 passed, 1 skipped**
- System diagnostics: **61 passed, 0 warnings, 0 failed**
- Deterministic release verification: **PASS**
- MaryV2 convergence: **PASS**
- Repository structure: **PASS**
- Release hygiene: **PASS**
- Standalone source readiness: **PASS**
- Local tool safety: **PASS**

A fresh Vite production bundle was not rebuilt inside the packaging container because npm dependency reconstruction timed out before Vite was available. JavaScript syntax checks pass. `NEXT_MAC_STEPS.md` requires `npm ci`, `npm run check`, and `npm run build` on the Mac before host packaging.

## Integrated synthesis mechanisms

- shared realtime/stream social-floor `SpeakerScheduler`;
- `REACT / NOTE / DROP` ambient attention with one-shot peripheral-awareness digest;
- bounded cross-surface `elsewhere, just now` stage awareness;
- session-only stream audience familiarity that never becomes relationship truth;
- dynamic current-valid action windows with revision/disposable-action semantics;
- safe shared `RealtimeDecisionTrace` visible on desktop/mobile as **Why Mary Did That**;
- confirmed-VAD gate before barge-in, with false-start tracking;
- readiness-aware capability-node routing;
- semantic motion library tied to existing `PerformancePacket` semantics;
- desktop VRM now physically consumes semantic motion cues instead of ignoring them;
- reproducible llama.cpp model/LoRA matrix runner;
- blinded creator Adapter Lab review plus literal machine-regression checks;
- explicit separation of human Mary-fit rubrics from literal output assertions;
- expanded architecture research/adoption ledger covering Neuro SDK, AIRI, ProjectBEA, Lumi/Nox, Open-LLM-VTuber, Warashi, Riko, YuriOS, Chasm, MotionMind, Project Gabriel, Neocortex Unity SDK, Emora, Rexclaw, Meuxe, Chati and AITuber OnAir.

## Canonical-owner rule

No imported concept may create a second identity, memory, relationship, character, cognition, autonomy, attention, speech, provider, or world-state authority. External projects are research material; useful mechanisms are integrated into Mary's existing owners and retained only if convergence/release gates remain green.

## Live validation still required

The deterministic gate does not prove experiential quality. On the Mac/Windows/stream hosts, still validate real model/LoRA Mary-fit, llama.cpp latency, VAD/barge-in under real audio, ElevenLabs performance, semantic body motion quality, Twitch/OBS reconnects, and future Unity/VRChat embodiment.
