# MaryV2 13.2 — Stage 14 TRUE Cumulative Compute-Fabric Convergence

This drop-in is cumulative from the authoritative `MaryV2-main current13.2.zip` baseline through the independently repaired Stage 13 full-spectrum convergence and the new Stage 14 compute-fabric convergence.

## Stage 14 result

Mary now exposes one display-safe compute-fabric contract spanning:

- configured cloud/local LLM routes,
- conversation/private/expert provider ordering,
- the last provider selection and failover attempts,
- purpose-specific selected model identity,
- connected replaceable capability nodes,
- bounded device-task state, and
- explicit `llm.ollama` route readiness.

This does **not** merge authority. Mary Core still owns identity, relationship, memory, character, agency, and developed self. The LLM router owns model-selection policy. Device nodes remain permission-bounded executors. Capability availability remains distinct from execution authorization.

## Preserved cumulative work

The package retains the corrected Stage 13 character realization, CharacterMind/local reflex path, character-aware reflection, Presence convergence, shared realtime lifecycle, performer packets for desktop/mobile, explicit training-feedback evidence, bounded remote Ollama capability-node bridge, and headless Windows node support.

## Verification

- Full pytest: **1292 passed, 1 skipped, 0 failed**.
- Focused LLM/distributed/Stage14 integration: **180 passed, 0 failed**.
- Python compileall: PASS.
- Desktop JS syntax: PASS.
- Mobile JS syntax: PASS.
- Mobile web/native JS + CSS synchronization: PASS.
- Checked-in `/data` SHA-256 tree hash unchanged: `a94448085099a8c97901394241f204718534e18ac9a74bbc76621e4cdba93d69`.
- `.env`, `/data`, caches, and bytecode are excluded.

Paste this archive over the authoritative 13.2 checkout. Preserve the live `.env` and live/canonical state exactly as before.
