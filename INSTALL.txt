MARYV2 V2 BREAKTHROUGH 10 - FULL SYSTEM CONSOLIDATION

PURPOSE
- Preserve the working MaryV2 architecture and rebuild its integration boundaries.
- Make personal/relational Mary conversation local-first through Ollama.
- Keep detached factual/technical/task work on the normal free-first cloud route.
- Keep private/offline work Ollama-only.
- Keep paid OpenAI explicit, one-task, advisory, and non-sticky.
- Connect explicit "ask me anything / you are here to learn" invitations to Mary's real relationship-curiosity gaps.
- Remove obvious test/probe residue from all normal model-facing creator/memory context while keeping it durable for /audit.
- Verify architecture authority top-down and provider/result flow bottom-up.

SAFE INSTALL
1. Exit Mary and the desktop app.
2. Commit/back up your current green project with GitHub Desktop.
3. Extract the upgrade/full-overwrite ZIP over the MaryV2 project root.
4. Replace matching source/test/docs files.
5. DO NOT delete or replace your existing .env, data/, API keys, avatar/VRM/Vroid assets, or local runtime state.

VERIFY
From the activated .venv in the project root:

    python -m scripts.verify_breakthrough_10
    python -m pytest tests/integration/test_breakthrough_10.py -q
    powershell -ExecutionPolicy Bypass -File scripts\hard_test_v2.ps1

TERMINAL ACCEPTANCE
Either command enters the same canonical runtime:

    python -m scripts.run_mary
    python run_mary.py

Useful commands:

    /state
    /route
    /contract
    /resources
    /audit
    /last

Real acceptance should use natural typing, not benchmark grammar.

NO personal .env, data, memories, API keys, or avatar source assets belong in the release overlay.
