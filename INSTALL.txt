MARYV2 END-TO-END RUNTIME ACCEPTANCE HOTFIX

Replace/add only the included files in the matching MaryV2 paths.
Do not replace .env, data/, memories, developed-self state, or runtime data.

This hotfix addresses two issues exposed by the real persistent-runtime acceptance session:
1. Natural compound questions such as
   "Who are you, and how do language models fit into your architecture?"
   now route to Mary's deterministic local runtime-architecture response.
2. Provider responses ending with an output-limit finish reason (for example
   finish_reason="length") are no longer accepted as successful dialogue.
   The router records the attempt as incomplete and continues failover.

Verification:
python -m pytest tests/integration/test_long_session_hardening.py tests/llm/test_provider_routing_guarantees.py -q
python -m scripts.verify_long_session_hardening
python -m scripts.verify_provider_routing_guarantees
python -m pytest tests -q
python -m scripts.run_release_verification --offline

Expected full-suite checkpoint after this hotfix:
408 passed, 1 skipped
43/43 diagnostics PASS
release gate PASS
