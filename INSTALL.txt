MaryV2 Provider Failover Stress + Routing Guarantees Patch

Replace/add ONLY these project files:

1. mary/llm/router.py                         REPLACE
2. tests/llm/test_provider_routing_guarantees.py   ADD
3. scripts/verify_multi_provider_router_install.py REPLACE
4. scripts/verify_provider_routing_guarantees.py   ADD

This patch does not contain or modify .env, API keys, data/, persistent memory,
developed-self state, or runtime data.

PowerShell verification from the MaryV2 repository root with .venv active:

python -m pytest tests/llm/test_provider_routing_guarantees.py -q
python -m scripts.verify_multi_provider_router_install
python -m scripts.verify_provider_routing_guarantees
python -m pytest tests -q
python -m scripts.run_diagnostics

Expected after this patch, based on the authoritative uploaded package:
- provider routing guarantee tests: 13 passed
- normal full test suite: 401 passed, 1 skipped
- diagnostics: 43/43 PASS, Warnings 0, Failed 0, Healthy True

Do NOT enable MARY_RUN_LIVE_TESTS during the deterministic regression above.
Run the controlled live smoke test separately only after the offline milestone is green.
