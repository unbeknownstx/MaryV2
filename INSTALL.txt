MARYV2 V2 RELEASE GATE CONSOLIDATION
====================================

Source checkpoint before this patch:
- 401 passed, 1 skipped
- 43/43 diagnostics PASS
- Provider Routing Guarantees PASS

FILES
-----
REPLACE:
  scripts/run_release_verification.py
  scripts/verify_performance_pass_install.py

ADD:
  tests/scripts/test_release_verification.py

Do not replace the project folder wholesale.
Do not modify .env, data/, memories, or runtime persistence files.

WHAT THIS CHANGES
-----------------
1. The release gate is deterministic/offline by default.
2. MARY_RUN_LIVE_TESTS is forcibly removed from the normal pytest child environment.
3. All 21 current verify_*.py milestone verifiers are explicitly registered.
4. A test guarantees new verifier scripts cannot silently be omitted from the gate.
5. Live LLM testing is separate and explicit with --live-llm.
6. Live web testing remains separate and explicit with --live-web.
7. The stale Performance Pass verifier is updated for the current TurnMindState/performance prompt shape.
8. Verifier CLI arguments are isolated so standalone argparse verifiers work inside the consolidated runner.

VERIFY IN VS CODE POWERSHELL
----------------------------
From the MaryV2 repository with .venv active:

python -m pytest tests/scripts/test_release_verification.py -q

Expected:
  5 passed

python -m scripts.verify_performance_pass_install

Expected final line:
  MARYV2 PERFORMANCE PASS V1 INSTALLED CORRECTLY

python -m pytest tests -q

Expected:
  406 passed, 1 skipped

python -m scripts.run_release_verification --offline

Expected final summary:
  every item PASS
  Release verification PASSED (deterministic/offline gate).

The release gate itself includes the 43/43 diagnostics check.

OPTIONAL LIVE CHECKS
--------------------
Do not use these for normal deterministic verification.

Dedicated live LLM smoke after the offline gate:
  python -m scripts.run_release_verification --live-llm

Explicit live web research after the offline gate:
  python -m scripts.run_release_verification --live-web

--offline cannot be combined with either live flag.
