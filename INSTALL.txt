MARYV2 V2 ACCEPTANCE HOTFIX 04

Purpose:
- Replaces the conversation provenance audit with exact normalized content-term matching.
- Prevents Mary-generated details from becoming creator history.
- Adds a versioned self-verifier so Python proves it imported the corrected reflection module.
- Registers the verifier in the permanent offline release gate.

Install:
1. Exit Mary.
2. Extract this ZIP over the MaryV2 project root and replace matching files.
3. Run:
   python -m scripts.verify_acceptance_hotfix_04
4. Run:
   python -m pytest tests/integration/test_long_session_hardening.py tests/cognition/test_conversation_continuity_v2.py -q
5. Optional full gate:
   powershell -ExecutionPolicy Bypass -File scripts\hard_test_v2.ps1

No .env, data, memories, avatar assets, or API keys are included.
